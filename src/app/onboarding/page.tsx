import OnboardingWizard from "@/src/features/onboarding/ui/OnboardingWizard";

/**
 * Page component that renders the onboarding wizard.
 *
 * @returns The page's JSX element containing a wrapper div with the onboarding wizard.
 */
export default function OnboardingPage() {
  return (
    <>
      <div>
        <OnboardingWizard />
      </div>
    </>
  );
}